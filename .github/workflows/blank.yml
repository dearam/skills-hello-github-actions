name: JayFlows
on:
  pull_request: 
    branches: ["main"]
  
jobs:
  build:
    name: flow
    runs-on: ubuntu-latest
    steps:
      - name: first
        uses: actions/checkout@v4
      - name: print
        run: echo "hello bhai"
      
    
  

  
